import { Mapper } from "../../../apis/mercadobitcoin/mapper"

const mapper = new Mapper()

describe("mapper parsing tests", () => {
    
    it("", () => {
        expect(mapper).toBe(mapper);
    });
})